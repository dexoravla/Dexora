# =====================================================================
# Dexora — runtime requirements (mirrors ``pyproject.toml`` ``dependencies``)
#
# Install in a fresh ``conda create -n dexora python=3.10`` env with:
#
#     # 1. PyTorch first, with the right CUDA tag for your machine.
#     pip install torch==2.1.0 torchvision==0.16.0 \
#         --index-url https://download.pytorch.org/whl/cu121
#
#     # 2. Then this file.
#     pip install -r requirements.txt
#
#     # 3. (Optional) flash-attn — Dexora's attention path falls back to
#     #    PyTorch SDPA without it, so this is purely a speed knob.
#     # pip install flash-attn --no-build-isolation
# =====================================================================

# --- Core training / optimization ---------------------------------------
packaging>=24.0
wandb>=0.17.0
accelerate>=0.30.0,<1.0
diffusers>=0.27.0,<0.32
timm>=1.0.0
# transformers 5.x dropped is_offline_mode; pin to 4.x.
transformers>=4.41.0,<5.0
# huggingface_hub 0.26 removed is_offline_mode and friends; transformers 4.x
# still imports them, so cap here.
huggingface-hub>=0.24.0,<0.26
sentencepiece>=0.2.0
safetensors>=0.4.0

# --- Dataset I/O --------------------------------------------------------
# LeRobot >=0.4 broke backward compatibility with the v2.1 dataset layout
# that the released Dexora_Real-World_Dataset uses.
lerobot>=0.3,<0.4
av
h5py>=3.11.0

# --- Image / numeric ----------------------------------------------------
opencv-python>=4.9.0
# imgaug accesses np.sctypes which was removed in NumPy 2.0; cap below 2.0.
imgaug>=0.4.0
numpy<2.0
Pillow>=10.0
matplotlib>=3.7

# --- General utils ------------------------------------------------------
pyyaml>=6.0
tqdm>=4.66
pymongo            # only required for the legacy BSON loader

# --- Real-robot deployment (deploy/) -----------------------------------
# Used by mmk_forwarder.py / xhand_forwarder.py / dexora_inference_zmq.py.
# Optional pyrealsense2 (for the RealSense head camera) is left to the user.
pyzmq>=25.0
